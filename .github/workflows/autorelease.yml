name: Auto-release

on:
  schedule:
    - cron: '0 0 * * 0'  # weekly
  workflow_dispatch:
  pull_request_target:
    types: [closed]

permissions:
  contents: write
  pull-requests: write

jobs:
  autorelease:
    uses: ivoronin/github-workflows/.github/workflows/autorelease.yml@da9a532c23890a7b70f130239a0abc93a268cdbe # main
    with:
      language: go
    secrets: inherit
